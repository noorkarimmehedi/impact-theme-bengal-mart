{% doc %}
  @prompt
    genertae a section like Delivery
    Enter Delivery Pincode
    CheckEnter pincode
    Delivery by13 Nov, Thursday?
    if ordered before 11:41 PM but time and date should be in real time. also instead of pincode customer need to put their City name
{% enddoc %}
{% assign ai_gen_id = block.id | replace: '_', '' | downcase %}

{% style %}
  .ai-delivery-checker-{{ ai_gen_id }} {
    padding: {{ block.settings.padding }}px;
    background-color: {{ block.settings.background_color }};
    border: 1px solid {{ block.settings.border_color }};
    border-radius: {{ block.settings.border_radius }}px;
  }

  .ai-delivery-checker__heading-{{ ai_gen_id }} {
    font-size: {{ block.settings.heading_size }}px;
    color: {{ block.settings.text_color }};
    margin: 0 0 16px 0;
    font-weight: 600;
  }

  .ai-delivery-checker__form-{{ ai_gen_id }} {
    display: flex;
    gap: 8px;
    margin-bottom: 16px;
  }

  .ai-delivery-checker__input-{{ ai_gen_id }} {
    flex: 1;
    padding: 12px 16px;
    border: 1px solid {{ block.settings.input_border_color }};
    border-radius: {{ block.settings.input_border_radius }}px;
    font-size: 14px;
    color: {{ block.settings.text_color }};
  }

  .ai-delivery-checker__input-{{ ai_gen_id }}::placeholder {
    color: {{ block.settings.placeholder_color }};
  }

  .ai-delivery-checker__button-{{ ai_gen_id }} {
    padding: 12px 24px;
    background-color: {{ block.settings.button_color }};
    color: {{ block.settings.button_text_color }};
    border: none;
    border-radius: {{ block.settings.button_border_radius }}px;
    font-size: 14px;
    font-weight: 600;
    cursor: pointer;
    white-space: nowrap;
    transition: background-color 0.3s ease;
  }

  .ai-delivery-checker__button-{{ ai_gen_id }}:hover {
    background-color: {{ block.settings.button_hover_color }};
  }

  .ai-delivery-checker__result-{{ ai_gen_id }} {
    display: none;
    padding: 16px;
    background-color: {{ block.settings.result_background }};
    border-radius: {{ block.settings.border_radius }}px;
    color: {{ block.settings.text_color }};
  }

  .ai-delivery-checker__result-{{ ai_gen_id }}.active {
    display: block;
  }

  .ai-delivery-checker__delivery-info-{{ ai_gen_id }} {
    display: flex;
    align-items: center;
    gap: 8px;
    font-size: 16px;
    font-weight: 600;
    margin-bottom: 8px;
  }

  .ai-delivery-checker__delivery-icon-{{ ai_gen_id }} {
    width: 20px;
    height: 20px;
    color: {{ block.settings.success_color }};
  }

  .ai-delivery-checker__delivery-date-{{ ai_gen_id }} {
    color: {{ block.settings.success_color }};
  }

  .ai-delivery-checker__condition-{{ ai_gen_id }} {
    font-size: 13px;
    color: {{ block.settings.text_color }};
    opacity: 0.8;
    margin-left: 28px;
  }

  .ai-delivery-checker__error-{{ ai_gen_id }} {
    display: none;
    padding: 12px 16px;
    background-color: {{ block.settings.error_background }};
    color: {{ block.settings.error_color }};
    border-radius: {{ block.settings.border_radius }}px;
    font-size: 14px;
  }

  .ai-delivery-checker__error-{{ ai_gen_id }}.active {
    display: block;
  }

  @media screen and (max-width: 749px) {
    .ai-delivery-checker__form-{{ ai_gen_id }} {
      flex-direction: column;
    }

    .ai-delivery-checker__button-{{ ai_gen_id }} {
      width: 100%;
    }
  }
{% endstyle %}

<delivery-checker-{{ ai_gen_id }}
  class="ai-delivery-checker-{{ ai_gen_id }}"
  data-delivery-days="{{ block.settings.delivery_days }}"
  data-cutoff-hour="{{ block.settings.cutoff_hour }}"
  data-cutoff-minute="{{ block.settings.cutoff_minute }}"
  {{ block.shopify_attributes }}
>
  <h3 class="ai-delivery-checker__heading-{{ ai_gen_id }}">{{ block.settings.heading }}</h3>

  <div class="ai-delivery-checker__form-{{ ai_gen_id }}">
    <input
      type="text"
      class="ai-delivery-checker__input-{{ ai_gen_id }}"
      placeholder="{{ block.settings.placeholder }}"
      aria-label="Enter your city name"
    >
    <button
      type="button"
      class="ai-delivery-checker__button-{{ ai_gen_id }}"
    >
      {{ block.settings.button_text }}
    </button>
  </div>

  <div class="ai-delivery-checker__result-{{ ai_gen_id }}">
    <div class="ai-delivery-checker__delivery-info-{{ ai_gen_id }}">
      <svg
        class="ai-delivery-checker__delivery-icon-{{ ai_gen_id }}"
        xmlns="http://www.w3.org/2000/svg"
        viewBox="0 0 24 24"
        fill="none"
        stroke="currentColor"
        stroke-width="2"
        stroke-linecap="round"
        stroke-linejoin="round"
      >
        <polyline points="20 6 9 17 4 12"></polyline>
      </svg>
      <span>
        Delivery by
        <span class="ai-delivery-checker__delivery-date-{{ ai_gen_id }}"></span>
      </span>
    </div>
    <div class="ai-delivery-checker__condition-{{ ai_gen_id }}">
      if ordered before <span class="ai-delivery-checker__cutoff-time-{{ ai_gen_id }}"></span>
    </div>
  </div>

  <div class="ai-delivery-checker__error-{{ ai_gen_id }}">
    {{ block.settings.error_message }}
  </div>
</delivery-checker-{{ ai_gen_id }}>

<script>
  (function() {
    class DeliveryChecker{{ ai_gen_id }} extends HTMLElement {
      constructor() {
        super();
        this.input = this.querySelector('.ai-delivery-checker__input-{{ ai_gen_id }}');
        this.button = this.querySelector('.ai-delivery-checker__button-{{ ai_gen_id }}');
        this.result = this.querySelector('.ai-delivery-checker__result-{{ ai_gen_id }}');
        this.error = this.querySelector('.ai-delivery-checker__error-{{ ai_gen_id }}');
        this.deliveryDateEl = this.querySelector('.ai-delivery-checker__delivery-date-{{ ai_gen_id }}');
        this.cutoffTimeEl = this.querySelector('.ai-delivery-checker__cutoff-time-{{ ai_gen_id }}');
        
        this.deliveryDays = parseInt(this.dataset.deliveryDays) || 3;
        this.cutoffHour = parseInt(this.dataset.cutoffHour) || 23;
        this.cutoffMinute = parseInt(this.dataset.cutoffMinute) || 41;
      }

      connectedCallback() {
        this.updateCutoffTime();
        this.button.addEventListener('click', () => this.checkDelivery());
        this.input.addEventListener('keypress', (e) => {
          if (e.key === 'Enter') {
            this.checkDelivery();
          }
        });
      }

      updateCutoffTime() {
        const now = new Date();
        const cutoffTime = new Date(now);
        cutoffTime.setHours(this.cutoffHour, this.cutoffMinute, 0, 0);
        
        const hours = cutoffTime.getHours();
        const minutes = cutoffTime.getMinutes();
        const ampm = hours >= 12 ? 'PM' : 'AM';
        const displayHours = hours % 12 || 12;
        const displayMinutes = minutes.toString().padStart(2, '0');
        
        this.cutoffTimeEl.textContent = `${displayHours}:${displayMinutes} ${ampm}`;
      }

      checkDelivery() {
        const city = this.input.value.trim();
        
        if (!city) {
          this.showError();
          return;
        }

        this.hideError();
        this.calculateDeliveryDate();
        this.showResult();
      }

      calculateDeliveryDate() {
        const now = new Date();
        const cutoffTime = new Date(now);
        cutoffTime.setHours(this.cutoffHour, this.cutoffMinute, 0, 0);
        
        let deliveryDate = new Date(now);
        
        if (now > cutoffTime) {
          deliveryDate.setDate(deliveryDate.getDate() + this.deliveryDays + 1);
        } else {
          deliveryDate.setDate(deliveryDate.getDate() + this.deliveryDays);
        }
        
        const day = deliveryDate.getDate();
        const monthNames = ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'];
        const month = monthNames[deliveryDate.getMonth()];
        const dayNames = ['Sunday', 'Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday'];
        const dayName = dayNames[deliveryDate.getDay()];
        
        this.deliveryDateEl.textContent = `${day} ${month}, ${dayName}`;
      }

      showResult() {
        this.result.classList.add('active');
      }

      showError() {
        this.error.classList.add('active');
        this.result.classList.remove('active');
      }

      hideError() {
        this.error.classList.remove('active');
      }
    }

    customElements.define('delivery-checker-{{ ai_gen_id }}', DeliveryChecker{{ ai_gen_id }});
  })();
</script>

{% schema %}
{
  "name": "Delivery checker",
  "tag": null,
  "settings": [
    {
      "type": "header",
      "content": "Content"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Delivery"
    },
    {
      "type": "text",
      "id": "placeholder",
      "label": "Input placeholder",
      "default": "Enter Delivery City"
    },
    {
      "type": "text",
      "id": "button_text",
      "label": "Button text",
      "default": "Check"
    },
    {
      "type": "text",
      "id": "error_message",
      "label": "Error message",
      "default": "Please enter your city name"
    },
    {
      "type": "header",
      "content": "Delivery settings"
    },
    {
      "type": "range",
      "id": "delivery_days",
      "min": 1,
      "max": 14,
      "step": 1,
      "unit": "d",
      "label": "Delivery days",
      "default": 3
    },
    {
      "type": "range",
      "id": "cutoff_hour",
      "min": 0,
      "max": 23,
      "step": 1,
      "unit": "h",
      "label": "Cutoff hour (24h format)",
      "default": 23
    },
    {
      "type": "range",
      "id": "cutoff_minute",
      "min": 0,
      "max": 59,
      "step": 1,
      "unit": "m",
      "label": "Cutoff minute",
      "default": 41
    },
    {
      "type": "header",
      "content": "Style"
    },
    {
      "type": "range",
      "id": "padding",
      "min": 0,
      "max": 40,
      "step": 4,
      "unit": "px",
      "label": "Padding",
      "default": 20
    },
    {
      "type": "range",
      "id": "heading_size",
      "min": 14,
      "max": 24,
      "step": 1,
      "unit": "px",
      "label": "Heading size",
      "default": 18
    },
    {
      "type": "range",
      "id": "border_radius",
      "min": 0,
      "max": 20,
      "step": 2,
      "unit": "px",
      "label": "Border radius",
      "default": 8
    },
    {
      "type": "range",
      "id": "input_border_radius",
      "min": 0,
      "max": 20,
      "step": 2,
      "unit": "px",
      "label": "Input border radius",
      "default": 8
    },
    {
      "type": "range",
      "id": "button_border_radius",
      "min": 0,
      "max": 20,
      "step": 2,
      "unit": "px",
      "label": "Button border radius",
      "default": 8
    },
    {
      "type": "header",
      "content": "Colors"
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Background",
      "default": "#f9f9f9"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text",
      "default": "#000000"
    },
    {
      "type": "color",
      "id": "border_color",
      "label": "Border",
      "default": "#e0e0e0"
    },
    {
      "type": "color",
      "id": "input_border_color",
      "label": "Input border",
      "default": "#cccccc"
    },
    {
      "type": "color",
      "id": "placeholder_color",
      "label": "Placeholder",
      "default": "#999999"
    },
    {
      "type": "color",
      "id": "button_color",
      "label": "Button background",
      "default": "#1a1a1a"
    },
    {
      "type": "color",
      "id": "button_text_color",
      "label": "Button text",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "button_hover_color",
      "label": "Button hover",
      "default": "#333333"
    },
    {
      "type": "color",
      "id": "result_background",
      "label": "Result background",
      "default": "#e8f5e9"
    },
    {
      "type": "color",
      "id": "success_color",
      "label": "Success",
      "default": "#00a341"
    },
    {
      "type": "color",
      "id": "error_background",
      "label": "Error background",
      "default": "#ffebee"
    },
    {
      "type": "color",
      "id": "error_color",
      "label": "Error text",
      "default": "#f83a3a"
    }
  ],
  "presets": [
    {
      "name": "Delivery checker"
    }
  ]
}
{% endschema %}